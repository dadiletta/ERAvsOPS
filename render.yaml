envVars:
  - key: PYTHON_VERSION
    value: 3.10.0
  - key: FLASK_ENV
    value: production
  - key: SECRET_KEY
    value: your-secret-key-here  # Use Render's UI for sensitive values
  - key: CACHE_TIMEOUT
    value: 3600
  # Add MLB_API_KEY if needed